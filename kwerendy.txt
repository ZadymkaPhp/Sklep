1 "SELECT * FROM produkt ORDER BY nazwa ASC";
2 SELECT z.id_zamowienia, CONCAT(k.imie, ' ', k.nazwisko) AS klient, k.email, z.status, z.kwota_calkowita FROM zamowienie z JOIN klient k ON z.id_klienta = k.id_klienta ORDER BY z.id_zamowienia ASC;
3